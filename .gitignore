# API keys and secrets
set_env.sh
*.env
.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Virtual environments
venv/
env/
ENV/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Data files (optional - uncomment if you don't want to track large CSVs)
# *.csv
# *.json

#PRD context for dev
/prd

#claude
/.claude
claude.md
nul

*.log
*.pre_retry_backup.csv
*.pre_tier2_backup.csv
failed_rins_*.txt

# Frozen snapshot CSV bytes stay local — manifests are committed, bytes are not
frozen/**/*.csv

# RTC parser I/O stays local: source PDF bytes under rtc/inputs/, and the
# derived crosswalk outputs under rtc/<slug>/ (regenerable from the parser + PDF;
# provenance is in each run's parse_manifest.json). The parser + committed text
# fixture (tests/fixtures/rtc_ccl5_slice.txt) define behavior.
rtc/
